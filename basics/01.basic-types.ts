const person: {
    name: string;
    age: number;
    hobbies: string[];
    tuple: [number, string]; // Tuple
    
} = {
    name: "Anh",
    age: 18,
    hobbies: ["1", "2", "111"],
    tuple: [1, 'aa'],
};

enum Role {
    ADMIN,
    USER
}
